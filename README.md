In the main folder, run `docker build -t applyvault-api .` in the console to create the build.
Note that for deploying, you can directly start the deploy in the render dashboard, it will read your Dockerfile and create a build.
